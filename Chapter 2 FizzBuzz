// FizzBuzz

for (var x = 1; x <= 100; x++) {
  var output = "";
  if (x % 3 && x % 5)
    output += “FizzBuzz”;
  if (x % 3 === 0)
    output += "Fizz";
  if (x % 5 === 0)
    output += "Buzz";
  console.log(output || x);
}
